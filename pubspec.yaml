name: news_app
description: A simple app used to fetch latest news from all over the world using the newsapi.org endpoint.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ">=2.16.1 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.2
  cupertino_icons: ^1.0.2
  http: ^0.13.5
  url_launcher: ^6.1.6
  share_plus: ^6.0.1
  path_provider: ^2.0.11
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/images/
